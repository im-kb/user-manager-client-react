import {Button} from "react-bootstrap";

const DeleteButton = ({user}) => {
    return (
        <Button variant="secondary" >       <i
            className="fa fa-times "></i></Button>
    )
}
export default DeleteButton
